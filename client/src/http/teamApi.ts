/*
import {$authHost, $host} from "./index";
import axios from "axios";

export const fetchTeams = async () => {
  const {data} = axios.get('${PUBLIC_API_URL}/api/team/get-all-team-info')
  return data
}*/
